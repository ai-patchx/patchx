version: '3.8'

services:
  ssh-service-api:
    build: .
    container_name: ssh-service-api
    ports:
      - "7000:7000"
    environment:
      - PORT=7000
      - API_KEY=${API_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-*}
    restart: unless-stopped
    networks:
      - ssh-service-network

networks:
  ssh-service-network:
    driver: bridge
